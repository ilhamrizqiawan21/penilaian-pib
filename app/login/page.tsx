"use client";
import {useState} from "react";
export default function Login(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});if(r.ok)location.href="/dashboard";else setError((await r.json()).error)}return <main className="app"><section className="card" style={{maxWidth:430,margin:"60px auto"}}><h1>Login guru</h1><form onSubmit={submit}><p><input style={{width:"100%",padding:12}} type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/></p><p><input style={{width:"100%",padding:12}} type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/></p>{error&&<p>{error}</p>}<button className="primary">Masuk</button></form></section></main>}
