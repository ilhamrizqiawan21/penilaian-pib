"use client";
import {useState} from "react";import {Alert} from "@/app/ui";
export default function Login(){const [email,setEmail]=useState(""),[password,setPassword]=useState(""),[show,setShow]=useState(false),[error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});setBusy(false);if(r.ok)location.href="/dashboard";else setError((await r.json()).error)}return <main className="app"><section className="card"><p className="eyebrow">PIB Penilaian</p><h1>Masuk sebagai guru</h1><p className="subtitle">Gunakan akun guru untuk mengelola data dan penilaian lokal.</p><form onSubmit={submit} className="section-stack" style={{marginTop:24}}><div className="field"><label htmlFor="email">Email</label><input id="email" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label htmlFor="password">Password</label><div className="actions"><input id="password" type={show?"text":"password"} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required/><button type="button" onClick={()=>setShow(!show)}>{show?"Sembunyikan":"Tampilkan"}</button></div></div>{error&&<Alert type="error">{error}</Alert>}<button className="primary" disabled={busy}>{busy?"Memeriksa akun…":"Masuk"}</button></form></section></main>}
