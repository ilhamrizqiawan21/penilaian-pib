import {afterEach,describe,expect,it,vi} from "vitest";
import {ApiError,api,errorMessage,jsonRequest} from "./client-api";
describe("client api",()=>{afterEach(()=>vi.unstubAllGlobals());it("returns typed JSON for successful responses",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue(new Response(JSON.stringify({ok:true}),{headers:{"content-type":"application/json"}})));await expect(api<{ok:boolean}>("/api/test")).resolves.toEqual({ok:true})});it("throws the API message and field errors",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue(new Response(JSON.stringify({error:"Tidak valid",fieldErrors:{name:["Wajib"]}}),{status:422,headers:{"content-type":"application/json"}})));await expect(api("/api/test")).rejects.toMatchObject({message:"Tidak valid",status:422,fieldErrors:{name:["Wajib"]}})});it("builds JSON requests and safe fallback messages",()=>{expect(jsonRequest("POST",{name:"A"})).toMatchObject({method:"POST",body:'{"name":"A"}'});expect(errorMessage(new ApiError("Gagal"))).toBe("Gagal")})});
